<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.savecreator.dao.NoticeDao">
	
	<select id="getBoard" resultType="com.savecreator.web.entity">
		select * from Board
		where id = #{param1}
	</select>
	
	<select id="getNoticeList" resultType="com.savecreator.web.entity">
		select * from Board
	</select>
	
</mapper>